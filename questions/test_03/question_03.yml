type: ChoiceQuestion
id: question_03_03
value: 2
prompt: |
    # Чему равен дифференциал $df(x)$ функции $f(x) = a^\top x$ для заданного $a\in\mathbb{R}^n$?
choices:

  - $a$
  - ~CORRECT~ $a^\top dx$
  - $a^\top$
  - $2a$

